# No external dependencies required for the code execution chatbot
# The server uses only Python standard library modules

# Optional: If you want to add more features in the future
# requests>=2.25.0
# flask>=2.0.0
